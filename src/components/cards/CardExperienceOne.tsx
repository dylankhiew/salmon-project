import { COLOR_CONSTANTS } from "../../constants/appConstants";
import Card from "../Card";

const CardExperienceOne = () => {
  return (
    <Card backgroundColor={COLOR_CONSTANTS.ACCENT}>
      
    </Card>
  )
};

export default CardExperienceOne;